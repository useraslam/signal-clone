import icons from './icons';
import images from './images';
import PhoneData from './countrydata.json';
import theme, {
  COLORS,
  SIZES,
  FONTS,
  darkTheme,
  lightTheme,
  selectedTheme,
} from './theme';

export {
  icons,
  images,
  theme,
  COLORS,
  SIZES,
  FONTS,
  darkTheme,
  lightTheme,
  selectedTheme,
  PhoneData,
};
